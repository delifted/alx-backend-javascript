export default function taskBlock(trueOrFalse) {
  const task = false;
  const task2 = true;

  if (trueOrFalse) {
    //    const newTask = true; // eslint-disable-line error - no unused vars
    //    const newTask2 = false; // eslint-disable-line error - no unused vars
  }

  return [task, task2];
}
